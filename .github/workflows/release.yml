# Builds, tags, and publishes a validated Study Planner release on demand.
name: Release

on:
  workflow_dispatch:
    inputs:
      version:
        description: "Semantic version matching package metadata"
        required: true
        default: "1.0.0"

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - run: npm ci
      - run: node scripts/validate-version.mjs "${{ inputs.version }}"
      - run: npm test
      - name: Create tag
        env:
          VERSION: ${{ inputs.version }}
        run: |
          if git rev-parse "$VERSION" >/dev/null 2>&1; then
            echo "Tag $VERSION already exists."
            exit 1
          fi
          git tag "$VERSION"
          git push origin "$VERSION"
      - name: Publish release assets
        env:
          GH_TOKEN: ${{ github.token }}
          VERSION: ${{ inputs.version }}
        run: gh release create "$VERSION" main.js manifest.json styles.css --title "Study Planner $VERSION" --generate-notes
